<config xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance'
        xmlns='http://www.ehcache.org/v3'
        xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd">

    <cache-template name="expiryCache">
        <expiry>
            <ttl unit="minutes">60</ttl>
        </expiry>
        <heap>500</heap>
    </cache-template>

    <cache-template name="singleNonExpiryCache">
        <expiry>
            <none/>
        </expiry>
        <heap>1</heap>
    </cache-template>

    <cache alias="menu" uses-template="expiryCache"/>
    <cache alias="menus" uses-template="expiryCache"/>
    <cache alias="voteDistribution" uses-template="expiryCache"/>
</config>
